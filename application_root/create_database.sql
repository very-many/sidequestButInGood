create table user
(
    email        text primary key,
    display_name text,
    password     text
);

create table user2
(
    name        text primary key,
    display_name text,
    password     text
);